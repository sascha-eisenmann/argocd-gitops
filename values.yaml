argo-cd:
  configs:
    cm:
      url: https://argocd.local
      admin.enabled: "true"
      exec.enabled: "true"
      application.links: |
        - url: {{ .resource.source.repoURL }}
          title: Github
  crds:
    keep: false